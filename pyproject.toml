[project]
name = "practica"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "behave>=1.3.3",
    "behave-django>=1.9.0",
    "django>=6.0.2",
    "mistune>=3.0",
    "psycopg[binary]>=3.2", # PostgreSQL driver for Django
    "splinter>=0.21.0",
]
